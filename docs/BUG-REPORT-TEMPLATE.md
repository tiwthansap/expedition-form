# แบบฟอร์มรายงานข้อผิดพลาด (Bug Report)

คัดลอกตารางด้านล่างไปกรอก 1 ชุดต่อ 1 ข้อผิดพลาดที่พบ

---

| หัวข้อ | รายละเอียด |
|---|---|
| **Bug ID** | BUG-001 |
| **หัวข้อ (Summary)** | เขียนสั้น ๆ ให้เห็นภาพในบรรทัดเดียว |
| **ผู้รายงาน** | ชื่อผู้ทดสอบ |
| **วันที่พบ** | dd/mm/yyyy |
| **หน้าจอ/ฟังก์ชัน** | เช่น ฟอร์มสมัคร → ช่องเบอร์โทรศัพท์ |
| **สภาพแวดล้อม** | เบราว์เซอร์ + เวอร์ชัน + ระบบปฏิบัติการ |
| **ความรุนแรง (Severity)** | Critical / High / Medium / Low |
| **ลำดับความสำคัญ (Priority)** | High / Medium / Low |
| **เทคนิคที่ใช้ทดสอบ** | Equivalence Partitioning / Boundary Value Analysis / อื่น ๆ |
| **ขั้นตอนการทำซ้ำ (Steps to Reproduce)** | 1. …<br>2. …<br>3. … |
| **ข้อมูลนำเข้าที่ใช้ (Test Data)** | ค่าที่กรอกจริง |
| **ผลที่คาดหวัง (Expected Result)** | ตามสเปกที่ประกาศบนหน้าเว็บ |
| **ผลที่เกิดขึ้นจริง (Actual Result)** | สิ่งที่ระบบทำจริง |
| **หลักฐาน (Evidence)** | ภาพหน้าจอ / วิดีโอ |
| **สถานะ (Status)** | New / Confirmed / Fixed / Rejected |

---

## ตัวอย่างการกรอก

| หัวข้อ | รายละเอียด |
|---|---|
| **Bug ID** | BUG-099 |
| **หัวข้อ** | ช่องอีเมลยอมรับที่อยู่ที่ไม่มีโดเมน |
| **ผู้รายงาน** | สมหญิง รักเรียน |
| **วันที่พบ** | 31/08/2026 |
| **หน้าจอ/ฟังก์ชัน** | ฟอร์มสมัคร → ช่องอีเมล |
| **สภาพแวดล้อม** | Chrome 128 / Windows 11 |
| **ความรุนแรง** | Medium |
| **ลำดับความสำคัญ** | Medium |
| **เทคนิคที่ใช้ทดสอบ** | Equivalence Partitioning |
| **ขั้นตอนการทำซ้ำ** | 1. เปิดหน้าฟอร์ม<br>2. กรอกอีเมลว่า `abc@abc`<br>3. คลิกออกจากช่อง |
| **ข้อมูลนำเข้าที่ใช้** | `abc@abc` |
| **ผลที่คาดหวัง** | ขึ้นข้อความ "รูปแบบอีเมลไม่ถูกต้อง เช่น name@example.com" |
| **ผลที่เกิดขึ้นจริง** | ช่องเปลี่ยนเป็นสถานะผ่าน (กรอบเขียว) |
| **หลักฐาน** | screenshot-099.png |
| **สถานะ** | New |

> หมายเหตุ: ตัวอย่างข้างบนเป็นเพียงรูปแบบการกรอกเท่านั้น ไม่ใช่ข้อผิดพลาดที่มีอยู่จริงในระบบนี้
